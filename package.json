{
  "name": "meta-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Meta-Re ↔ KAYAK Affiliate API 중계(프록시) 서버",
  "engines": {
    "node": ">=20.12"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"test/**/*.test.js\"",
    "test:connect": "node scripts/connection-test.mjs",
    "test:route": "node scripts/route-test.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0"
  }
}
